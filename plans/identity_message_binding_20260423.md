# 多身份消息归属修复计划（2026-04-23）

## 目标
- 修复多身份场景下回包误归属问题。
- 为每条已发送命令绑定发送身份，优先按命令消息 ID 归属系统回包。
- 日志与消息归档能看出命令由主魂还是哪个化身发出。

## 方案
- [x] 梳理发送链路、回包归属链路、消息归档结构。
- [x] 增加发送消息 ID 到 `identity_key/plugin` 的运行时映射。
- [x] 收到系统回包时优先按 `reply_to_msg_id` 命中发送映射选身份。
- [x] 发送日志追加身份信息。
- [x] 消息归档增加身份字段并展示。
- [x] 补测试覆盖：主魂任务在化身激活时仍能正确切回；归档展示身份。
