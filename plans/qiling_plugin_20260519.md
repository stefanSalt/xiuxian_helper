# 器灵插件计划 20260519

## 归档取证

- [x] 从 rn1 `message_archive` 只读抽样器灵相关消息。
- [x] 确认命令：
  - `.我的器灵`
  - `.器灵 <法宝名>`
  - `.抚摸法宝 <法宝名>`
  - `.温养器灵 <法宝名>`
  - `.器灵试炼 <法宝名> [静修|寻宝|斗战]`
  - `.器灵护主 <法宝名>`
  - `.唤醒器灵 <法宝名>`
- [x] 确认主要冷却：
  - 抚摸：约 2 小时，回包含 `器灵也是需要休息的，请在 ... 后再与它互动。`
  - 温养：约 6 小时，回包含 `请在 ... 后再行温养。`
  - 试炼：约 8 小时，回包含 `请在 ... 后再启程。`
  - 护主：约 12 小时，回包含 `请在 ... 后再请它护主。` 或 `请在 ... 后再行催发。`

## 建议 MVP

- [x] 新增 `QilingPlugin`，默认关闭。
- [x] 新增全局配置和身份覆盖开关：`enable_qiling`。
- [x] 新增基础配置：
  - `qiling_artifact_names`：逗号分隔法宝名；留空时先用 `.我的器灵` 自动发现。
  - `qiling_enable_touch`：是否自动抚摸，独立开关。
  - `qiling_enable_nurture`：是否自动温养，独立开关。
  - `qiling_enable_trial`：是否自动静修试炼，独立开关。
  - `qiling_trial_route`：默认 `静修`。
  - 护主本期仅解析状态冷却，不新增自动执行开关。
- [x] 接入 `build_plugins()`、Web 表单、身份配置覆盖。
- [x] 状态持久化每个法宝的 touch/nurture/trial/protect 下一次可用时间。
- [x] 解析 `.我的器灵` 和 `.器灵 <法宝名>` 回包中的法宝名、状态和各动作可用/等待时间。
- [x] 解析动作成功、冷却、资源不足、法宝名错误回包。
- [x] 加测试覆盖自动发现、冷却回包、资源不足、按身份隔离。

## 已确认范围

- [x] 本期自动动作：抚摸、温养、静修试炼。
- [x] 抚摸、温养、静修试炼都必须有独立开关。
- [x] 法宝列表支持手动配置；未配置时从 `.我的器灵` 自动发现。
- [x] `.器灵护主` 本期不自动执行。
- [x] `.唤醒器灵` 本期不自动执行。

## 执行检查清单

- [x] 编写 `tests/test_qiling.py`，覆盖列表解析、自动发现、独立开关、冷却回包、资源不足。
- [x] 新增 `xiuxian_bot/plugins/qiling.py`。
- [x] 扩展 `Config` 默认值、`from_mapping()` 和环境变量读取。
- [x] 扩展 Web 全局配置和身份插件覆盖字段。
- [x] 接入 `runtime.build_plugins()`。
- [x] 运行相关单测和语法检查。
